---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: apikeyapprovals.devportal.kuadrant.io
spec:
  group: devportal.kuadrant.io
  names:
    kind: APIKeyApproval
    listKind: APIKeyApprovalList
    plural: apikeyapprovals
    singular: apikeyapproval
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: APIKeyApproval is the Schema for the apikeyapprovals API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: APIKeyApprovalSpec defines the desired state of APIKeyApproval.
            properties:
              apiKeyRequestRef:
                description: Reference to the APIKeyRequest
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              approved:
                description: Approved indicates whether the API key request is approved
                type: boolean
              message:
                description: Message provides additional context about the approval
                  or denial
                type: string
              reason:
                description: Reason for the approval or denial decision
                type: string
              reviewedAt:
                description: ReviewedAt is the timestamp when the request was reviewed
                format: date-time
                type: string
              reviewedBy:
                description: ReviewedBy contains the identifier of the person who
                  reviewed the request
                type: string
            required:
            - apiKeyRequestRef
            - approved
            - reviewedAt
            - reviewedBy
            type: object
          status:
            description: APIKeyApprovalStatus defines the observed state of APIKeyApproval.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
